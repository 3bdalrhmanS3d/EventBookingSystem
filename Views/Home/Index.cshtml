
    <!-- Main content of the homepage -->
    <div class="container">
        <main role="main" class="pb-3">
            <h1 class="mt-5 text-center">Welcome to Event Booking System</h1>
            <p class="lead text-center">Manage events, bookings, and users all in one place.</p>

            <!-- Buttons for sign in and sign up -->
            <div class="d-flex justify-content-center mt-4">
                <a href="@Url.Action("SignIn", "Account")" class="btn btn-primary btn-lg me-3">Sign In</a>
                <a href="@Url.Action("SignUp", "Account")" class="btn btn-success btn-lg">Sign Up</a>
            </div>

            

            <!-- Event showcase section -->
            <div class="mt-5">
                <h3>Upcoming Events</h3>
                
            </div>

        </main>
    </div>
